## What this changes

<!-- One line. For a new entry, paste the entry itself. -->

## If this adds something to the list

- [ ] The link resolves (the CI link check will confirm, but please look first)
- [ ] The description says what it does, not how good it is
- [ ] If the project is archived or dormant, the description says so
- [ ] It is in the section it belongs in, in a sensible position
- [ ] It is not already listed
- [ ] It is my own project — say so here if yes:

## If this changes code

- [ ] Web NFC uses `NDEFReader`, not the removed `NDEFWriter`
- [ ] Python samples use `ndeflib`, not the `nfc.ndef` module removed in nfcpy 1.0
- [ ] I tested it against real hardware, and said below what chip and reader

Tested on:

<!-- e.g. Pixel 8 / Chrome 141 / NTAG215, or ACR122U + nfcpy 1.0.4 -->
